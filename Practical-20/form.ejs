<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD Operations</title>
</head>
<body>
  <h1>Form</h1>
  <form action="<%= person ? '/update/' + person._id : '/create' %>" method="post">
    <label for="name">Name:</label>
    <input type="text" name="name" id="name" value="<%= person ? person.name : '' %>" required>
    <br>
    <label for="age">Age:</label>
    <input type="number" name="age" id="age" value="<%= person ? person.age : '' %>" required>
    <br>
    <label for="email">Email:</label>
    <input type="email" name="email" id="email" value="<%= person ? person.email : '' %>" required>
    <br>
    <button type="submit">Submit</button>
  </form>
</body>
</html>
